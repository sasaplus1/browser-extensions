{
  "devDependencies": {
    "@types/webextension-polyfill": "^0.12.0",
    "web-ext": "^8.0.0",
    "webextension-polyfill": "^0.12.0"
  },
  "private": true,
  "scripts": {
    "lint": "web-ext lint -s ./xpi",
    "run": "web-ext run -s ./xpi --devtools"
  }
}
