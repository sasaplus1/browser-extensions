{
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^3.0.3"
  },
  "engines": {
    "node": "20",
    "npm": "10"
  },
  "license": "MIT",
  "prettier": {
    "singleQuote": true,
    "trailingComma": "none"
  },
  "private": true,
  "scripts": {
    "fix": "run-p -l fix:*",
    "fix:prettier": "prettier --write .",
    "lint": "run-p -l lint:*",
    "lint:prettier": "prettier --check ."
  },
  "workspaces": [
    "extensions/*"
  ]
}
